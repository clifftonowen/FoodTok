Index: app/src/main/java/com/example/foodtok/util/ApiClient.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package com.example.foodtok.util;\n\nimport okhttp3.OkHttpClient;\nimport okhttp3.Request;\nimport okhttp3.logging.HttpLoggingInterceptor;\nimport retrofit2.Retrofit;\nimport retrofit2.converter.gson.GsonConverterFactory;\n\npublic final class ApiClient {\n\n  private static Retrofit restRetrofit;   // for table queries\n  private static Retrofit authRetrofit;   // for signup/login\n\n  private ApiClient() {\n    // prevent instantiation\n  }\n\n  // Shared OkHttpClient — attaches headers to EVERY request\n  private static OkHttpClient buildClient() {\n    // Logging — shows request/response in Logcat (debug only)\n    HttpLoggingInterceptor logging = new HttpLoggingInterceptor();\n    logging.setLevel(HttpLoggingInterceptor.Level.BODY);\n\n    return new OkHttpClient.Builder()\n        .addInterceptor(chain -> {\n          Request.Builder builder = chain.request().newBuilder()\n              // Every Supabase request needs the anon key\n              .addHeader(\"apikey\", Constants.SUPABASE_ANON_KEY)\n              .addHeader(\"Content-Type\", \"application/json\");\n\n          // If user is logged in, attach their JWT\n          String token = SessionManager.getInstance().getAccessToken();\n          if (token != null) {\n            builder.addHeader(\"Authorization\", \"Bearer \" + token);\n          }\n\n          // PostgREST: return the created/updated row in response\n          builder.addHeader(\"Prefer\", \"return=representation\");\n\n          return chain.proceed(builder.build());\n        })\n        .addInterceptor(logging)\n        .build();\n  }\n\n  // Retrofit instance for table CRUD (recipes, profiles, follows, etc.)\n  public static Retrofit getRestClient() {\n    if (restRetrofit == null) {\n      restRetrofit = new Retrofit.Builder()\n          .baseUrl(Constants.REST_BASE_URL)\n          .client(buildClient())\n          .addConverterFactory(GsonConverterFactory.create())\n          .build();\n    }\n    return restRetrofit;\n  }\n\n  // Retrofit instance for Auth (signup, login, logout)\n  public static Retrofit getAuthClient() {\n    if (authRetrofit == null) {\n      authRetrofit = new Retrofit.Builder()\n          .baseUrl(Constants.AUTH_BASE_URL)\n          .client(buildClient())\n          .addConverterFactory(GsonConverterFactory.create())\n          .build();\n    }\n    return authRetrofit;\n  }\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/app/src/main/java/com/example/foodtok/util/ApiClient.java b/app/src/main/java/com/example/foodtok/util/ApiClient.java
--- a/app/src/main/java/com/example/foodtok/util/ApiClient.java	(revision f0a5f093043d93f34e33e0add059953cf205f829)
+++ b/app/src/main/java/com/example/foodtok/util/ApiClient.java	(date 1775818267406)
@@ -8,40 +8,40 @@
 
 public final class ApiClient {
 
-  private static Retrofit restRetrofit;   // for table queries
-  private static Retrofit authRetrofit;   // for signup/login
+    private static Retrofit restRetrofit;   // for table queries
+    private static Retrofit authRetrofit;   // for signup/login
 
-  private ApiClient() {
-    // prevent instantiation
-  }
+    private ApiClient() {
+        // prevent instantiation
+    }
 
-  // Shared OkHttpClient — attaches headers to EVERY request
-  private static OkHttpClient buildClient() {
-    // Logging — shows request/response in Logcat (debug only)
-    HttpLoggingInterceptor logging = new HttpLoggingInterceptor();
-    logging.setLevel(HttpLoggingInterceptor.Level.BODY);
+    // Shared OkHttpClient — attaches headers to EVERY request
+    private static OkHttpClient buildClient() {
+        // Logging — shows request/response in Logcat (debug only)
+        HttpLoggingInterceptor logging = new HttpLoggingInterceptor();
+        logging.setLevel(HttpLoggingInterceptor.Level.BODY);
 
-    return new OkHttpClient.Builder()
-        .addInterceptor(chain -> {
-          Request.Builder builder = chain.request().newBuilder()
-              // Every Supabase request needs the anon key
-              .addHeader("apikey", Constants.SUPABASE_ANON_KEY)
-              .addHeader("Content-Type", "application/json");
+        return new OkHttpClient.Builder()
+                .addInterceptor(chain -> {
+                    Request.Builder builder = chain.request().newBuilder()
+                            // Every Supabase request needs the anon key
+                            .addHeader("apikey", Constants.SUPABASE_ANON_KEY)
+                            .addHeader("Content-Type", "application/json");
 
-          // If user is logged in, attach their JWT
-          String token = SessionManager.getInstance().getAccessToken();
-          if (token != null) {
-            builder.addHeader("Authorization", "Bearer " + token);
-          }
+                    // If user is logged in, attach their JWT
+                    String token = SessionManager.getInstance().getAccessToken();
+                    if (token != null) {
+                        builder.addHeader("Authorization", "Bearer " + token);
+                    }
 
-          // PostgREST: return the created/updated row in response
-          builder.addHeader("Prefer", "return=representation");
+                    // PostgREST: return the created/updated row in response
+                    builder.addHeader("Prefer", "return=representation");
 
-          return chain.proceed(builder.build());
-        })
-        .addInterceptor(logging)
-        .build();
-  }
+                    return chain.proceed(builder.build());
+                })
+                .addInterceptor(logging)
+                .build();
+    }
 
   // Retrofit instance for table CRUD (recipes, profiles, follows, etc.)
   public static Retrofit getRestClient() {
@@ -55,15 +55,15 @@
     return restRetrofit;
   }
 
-  // Retrofit instance for Auth (signup, login, logout)
-  public static Retrofit getAuthClient() {
-    if (authRetrofit == null) {
-      authRetrofit = new Retrofit.Builder()
-          .baseUrl(Constants.AUTH_BASE_URL)
-          .client(buildClient())
-          .addConverterFactory(GsonConverterFactory.create())
-          .build();
-    }
-    return authRetrofit;
-  }
+    // Retrofit instance for Auth (signup, login, logout)
+    public static Retrofit getAuthClient() {
+        if (authRetrofit == null) {
+            authRetrofit = new Retrofit.Builder()
+                    .baseUrl(Constants.AUTH_BASE_URL)
+                    .client(buildClient())
+                    .addConverterFactory(GsonConverterFactory.create())
+                    .build();
+        }
+        return authRetrofit;
+    }
 }
\ No newline at end of file
Index: app/src/main/java/com/example/foodtok/services/SupabaseAuthApi.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package com.example.foodtok.services;\n\nimport com.example.foodtok.models.dto.AuthResponse;\nimport com.example.foodtok.models.dto.LoginRequest;\nimport com.example.foodtok.models.dto.SignUpRequest;\n\nimport retrofit2.Call;\nimport retrofit2.http.Body;\nimport retrofit2.http.Header;\nimport retrofit2.http.POST;\nimport retrofit2.http.Query;\n\n/** Retrofit interface for Supabase GoTrue authentication endpoints. */\npublic interface SupabaseAuthApi {\n\n  // POST /auth/v1/signup\n  @POST(\"signup\")\n  Call<AuthResponse> signUp(@Body SignUpRequest request);\n\n  // POST /auth/v1/token?grant_type=password\n  @POST(\"token\")\n  Call<AuthResponse> login(\n      @Query(\"grant_type\") String grantType,\n      @Body LoginRequest request\n  );\n\n  // POST /auth/v1/logout\n  @POST(\"logout\")\n  Call<Void> logout(@Header(\"Authorization\") String token);\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/app/src/main/java/com/example/foodtok/services/SupabaseAuthApi.java b/app/src/main/java/com/example/foodtok/services/SupabaseAuthApi.java
--- a/app/src/main/java/com/example/foodtok/services/SupabaseAuthApi.java	(revision f0a5f093043d93f34e33e0add059953cf205f829)
+++ b/app/src/main/java/com/example/foodtok/services/SupabaseAuthApi.java	(date 1775818305000)
@@ -17,14 +17,14 @@
   @POST("signup")
   Call<AuthResponse> signUp(@Body SignUpRequest request);
 
-  // POST /auth/v1/token?grant_type=password
-  @POST("token")
-  Call<AuthResponse> login(
-      @Query("grant_type") String grantType,
-      @Body LoginRequest request
-  );
+    // POST /auth/v1/token?grant_type=password
+    @POST("token")
+    Call<AuthResponse> login(
+            @Query("grant_type") String grantType,
+            @Body LoginRequest request
+    );
 
-  // POST /auth/v1/logout
-  @POST("logout")
-  Call<Void> logout(@Header("Authorization") String token);
+    // POST /auth/v1/logout
+    @POST("logout")
+    Call<Void> logout(@Header("Authorization") String token);
 }
\ No newline at end of file
Index: app/src/main/java/com/example/foodtok/auth/SupabaseAuthService.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>package com.example.foodtok.auth;\n\nimport com.example.foodtok.models.User;\nimport com.example.foodtok.models.dto.AuthResponse;\nimport com.example.foodtok.models.dto.LoginRequest;\nimport com.example.foodtok.models.dto.SignUpRequest;\nimport com.example.foodtok.services.SupabaseAuthApi;\nimport com.example.foodtok.util.ApiClient;\nimport com.example.foodtok.util.SessionManager;\n\nimport retrofit2.Call;\nimport retrofit2.Callback;\nimport retrofit2.Response;\n\n/** Production {@link IAuthService} implementation using Supabase GoTrue via Retrofit. */\npublic class SupabaseAuthService implements IAuthService {\n\n  private final SupabaseAuthApi authApi;\n\n  public SupabaseAuthService() {\n    this.authApi = ApiClient.getAuthClient().create(SupabaseAuthApi.class);\n  }\n\n  @Override\n  public void signUp(String username, String email, String password,\n                       AuthCallback callback) {\n\n    SignUpRequest request = new SignUpRequest(email, password, username);\n\n    authApi.signUp(request).enqueue(new Callback<AuthResponse>() {\n      @Override\n      public void onResponse(Call<AuthResponse> call,\n                                   Response<AuthResponse> response) {\n\n        if (response.isSuccessful() && response.body() != null) {\n          handleAuthSuccess(response.body(), callback);\n        } else {\n          callback.onError(\"Sign up failed: \" + response.code());\n        }\n      }\n\n      @Override\n      public void onFailure(Call<AuthResponse> call, Throwable t) {\n        callback.onError(\"Network error: \" + t.getMessage());\n      }\n    });\n  }\n\n  @Override\n  public void login(String email, String password,\n           AuthCallback callback) {\n\n    LoginRequest request = new LoginRequest(email, password);\n\n    authApi.login(\"password\", request).enqueue(new Callback<AuthResponse>() {\n      @Override\n      public void onResponse(Call<AuthResponse> call,\n                                   Response<AuthResponse> response) {\n\n        if (response.isSuccessful() && response.body() != null) {\n          handleAuthSuccess(response.body(), callback);\n        } else {\n          callback.onError(\"Login failed: \" + response.code());\n        }\n      }\n\n      @Override\n      public void onFailure(Call<AuthResponse> call, Throwable t) {\n        callback.onError(\"Network error: \" + t.getMessage());\n      }\n    });\n  }\n\n  @Override\n  public void logout() {\n    // Clear local session first\n    SessionManager.getInstance().clearSession();\n    AuthManager.getInstance().logout();\n  }\n\n  // Shared logic for both signup and login success\n  private void handleAuthSuccess(AuthResponse authResponse,\n                                   AuthCallback callback) {\n\n    AuthResponse.AuthUser authUser = authResponse.getUser();\n\n    // 1. Save JWT to disk (survives app restart)\n    SessionManager.getInstance().saveSession(\n        authResponse.getAccessToken(),\n        authResponse.getRefreshToken(),\n        authUser.getId(),\n        authUser.getUsername()\n    );\n\n    // 2. Create domain User object\n    User user = new User(\n        authUser.getId(),\n        authUser.getUsername(),\n        authUser.getEmail()\n    );\n\n    // 3. Save to in-memory AuthManager\n    AuthManager.getInstance().login(user);\n\n    // 4. Tell the caller it worked\n    callback.onSuccess(user);\n  }\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/app/src/main/java/com/example/foodtok/auth/SupabaseAuthService.java b/app/src/main/java/com/example/foodtok/auth/SupabaseAuthService.java
--- a/app/src/main/java/com/example/foodtok/auth/SupabaseAuthService.java	(revision f0a5f093043d93f34e33e0add059953cf205f829)
+++ b/app/src/main/java/com/example/foodtok/auth/SupabaseAuthService.java	(date 1775818446970)
@@ -15,94 +15,94 @@
 /** Production {@link IAuthService} implementation using Supabase GoTrue via Retrofit. */
 public class SupabaseAuthService implements IAuthService {
 
-  private final SupabaseAuthApi authApi;
+    private final SupabaseAuthApi authApi;
 
-  public SupabaseAuthService() {
-    this.authApi = ApiClient.getAuthClient().create(SupabaseAuthApi.class);
-  }
+    public SupabaseAuthService() {
+        this.authApi = ApiClient.getAuthClient().create(SupabaseAuthApi.class);
+    }
 
-  @Override
-  public void signUp(String username, String email, String password,
+    @Override
+    public void signUp(String username, String email, String password,
                        AuthCallback callback) {
 
-    SignUpRequest request = new SignUpRequest(email, password, username);
+        SignUpRequest request = new SignUpRequest(email, password, username);
 
     authApi.signUp(request).enqueue(new Callback<AuthResponse>() {
       @Override
       public void onResponse(Call<AuthResponse> call,
                                    Response<AuthResponse> response) {
 
-        if (response.isSuccessful() && response.body() != null) {
-          handleAuthSuccess(response.body(), callback);
-        } else {
-          callback.onError("Sign up failed: " + response.code());
-        }
-      }
+                if (response.isSuccessful() && response.body() != null) {
+                    handleAuthSuccess(response.body(), callback);
+                } else {
+                    callback.onError("Sign up failed: " + response.code());
+                }
+            }
 
-      @Override
-      public void onFailure(Call<AuthResponse> call, Throwable t) {
-        callback.onError("Network error: " + t.getMessage());
-      }
-    });
-  }
+            @Override
+            public void onFailure(Call<AuthResponse> call, Throwable t) {
+                callback.onError("Network error: " + t.getMessage());
+            }
+        });
+    }
 
-  @Override
-  public void login(String email, String password,
-           AuthCallback callback) {
+    @Override
+    public void login(String email, String password,
+                      AuthCallback callback) {
 
-    LoginRequest request = new LoginRequest(email, password);
+        LoginRequest request = new LoginRequest(email, password);
 
-    authApi.login("password", request).enqueue(new Callback<AuthResponse>() {
-      @Override
-      public void onResponse(Call<AuthResponse> call,
+        authApi.login("password", request).enqueue(new Callback<AuthResponse>() {
+            @Override
+            public void onResponse(Call<AuthResponse> call,
                                    Response<AuthResponse> response) {
 
-        if (response.isSuccessful() && response.body() != null) {
-          handleAuthSuccess(response.body(), callback);
-        } else {
-          callback.onError("Login failed: " + response.code());
-        }
-      }
+                if (response.isSuccessful() && response.body() != null) {
+                    handleAuthSuccess(response.body(), callback);
+                } else {
+                    callback.onError("Login failed: " + response.code());
+                }
+            }
 
-      @Override
-      public void onFailure(Call<AuthResponse> call, Throwable t) {
-        callback.onError("Network error: " + t.getMessage());
-      }
-    });
-  }
+            @Override
+            public void onFailure(Call<AuthResponse> call, Throwable t) {
+                callback.onError("Network error: " + t.getMessage());
+            }
+        });
+    }
 
-  @Override
-  public void logout() {
-    // Clear local session first
-    SessionManager.getInstance().clearSession();
-    AuthManager.getInstance().logout();
-  }
+    @Override
+    public void logout() {
+        // Clear local session first
+        SessionManager.getInstance().clearSession();
+        AuthManager.getInstance().logout();
+    }
 
   // Shared logic for both signup and login success
   private void handleAuthSuccess(AuthResponse authResponse,
                                    AuthCallback callback) {
 
-    AuthResponse.AuthUser authUser = authResponse.getUser();
+        AuthResponse.AuthUser authUser = authResponse.getUser();
 
-    // 1. Save JWT to disk (survives app restart)
-    SessionManager.getInstance().saveSession(
-        authResponse.getAccessToken(),
-        authResponse.getRefreshToken(),
-        authUser.getId(),
-        authUser.getUsername()
-    );
+        // 1. Save JWT to disk (survives app restart)
+        SessionManager.getInstance().saveSession(
+                authResponse.getAccessToken(),
+                authResponse.getRefreshToken(),
+                authUser.getId(),
+                authUser.getUsername()
+        );
 
-    // 2. Create domain User object
-    User user = new User(
-        authUser.getId(),
-        authUser.getUsername(),
-        authUser.getEmail()
-    );
+        // 2. Create domain User object
+        User user = new User(
+                authUser.getId(),
+                authUser.getUsername(),
+                authUser.getEmail()
+        );
 
-    // 3. Save to in-memory AuthManager
-    AuthManager.getInstance().login(user);
+        // 3. Save to in-memory AuthManager
+        AuthManager.getInstance().login(user);
 
-    // 4. Tell the caller it worked
-    callback.onSuccess(user);
-  }
+        // 4. Tell the caller it worked
+        callback.onSuccess(user);
+    }
 }
\ No newline at end of file
